kind = "web"
previewPath = "/"
title = "SiteSort"
version = "1.0.0"
id = "artifacts/sitesort"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18299

[services.development]
run = "pnpm --filter @workspace/sitesort run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sitesort", "run", "build" ]
publicDir = "artifacts/sitesort/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18299"
BASE_PATH = "/"
